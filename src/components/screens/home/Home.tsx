import React from 'react';

import { Layout } from '../../layout/Layout';
import { Header } from './Header';

export const Home = () => {
    return (
        <Layout>
            <Header />
        </Layout>
    )
}